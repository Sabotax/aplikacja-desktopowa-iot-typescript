export class TableRow {
    time: string
    humidity: Number
    
    constructor(
        time: string,
        humidity: Number
    ) {
        this.time = time
        this.humidity = humidity
    }
}